# Compute-node and partition fragment used in the laboratory.
# Add it to a complete slurm.conf with the site-specific controller settings.
NodeName=worker1,worker2,worker3 CPUs=1 Sockets=1 CoresPerSocket=1 ThreadsPerCore=1 State=UNKNOWN
PartitionName=debug Nodes=ALL Default=YES MaxTime=INFINITE State=UP
